---
# Distro-agnostic defaults for the common role.
# Per-family overrides live in vars/{OsFamily}.yml and are loaded via
# first_found in tasks/main.yml.

common_base_packages_common:
  - curl
  - ca-certificates
  - gnupg
  - chrony
  - logrotate
  - jq
  - htop
  - ncdu
  - tmux
